---
description: 포트폴리오 초기화 — Next.js 생성부터 모든 스타일·페이지 생성까지 한 번에 완료 (첫 번째 실행)
argument-hint: "<프로젝트 이름 (선택, 기본: jione-portfolio)>"
---

🟢 **Phase 1 — 프로젝트 초기화 (All-in-One)**

Next.js 프로젝트를 만들고, **모든 UI 스타일과 톤앤매너를 한 번에** 생성합니다.
이 command 하나로 모든 `/{style}/{tone}` 라우트와 컴포넌트가 완성됩니다.
이 command는 **가장 먼저 실행되어야 합니다**.

## 목표

- Next.js 16 + React 19 프로젝트 구조 생성
- 필수 의존성 설치 (styled-components, Framer Motion 등)
- 공용 인프라 파일 생성 (provider.tsx, styled.d.ts, globals.css)
- **모든 스타일 컴포넌트 생성** (`2[a-z]*.md` 파일 전체 처리)
- **모든 색상 토큰 생성** (`3[a-z]*.md` 파일 전체 처리)
- **모든 라우트 생성** (style × tone 전체 조합)
- 환경 변수 템플릿, TypeScript/ESLint/Prettier 설정

---

## Steps

### 0. 실행 모드 선택 (필수)

`c:/work/jione-portfolio/` 폴더 존재 여부를 Bash 툴로 확인한 뒤, 아래 옵션을 사용자에게 제시하세요.

**폴더가 없는 경우** → 옵션 없이 바로 Step 1로 진행

**폴더가 이미 있는 경우** → 아래 두 옵션을 제시하고 선택을 기다리세요:

```
c:/work/jione-portfolio 폴더가 이미 존재합니다. 어떻게 진행할까요?

  1) 폴더 초기화 후 처음부터 재생성
           · jione-portfolio 내 파일을 모두 삭제합니다 (.git 유지)
           · npx create-next-app → 의존성 설치 → 전체 파일 재생성
           · ⚠️ npm run dev가 실행 중이라면 먼저 중지(Ctrl+C)해 주세요

  2) 기존 프로젝트 유지, 콘텐츠만 재설정
           · create-next-app·npm install 단계를 건너뜁니다
           · /theme-on 부터 재실행합니다
```

- **옵션 1 선택 시**: 삭제 전 `.env.local`을 **portfolio-agent 루트에 백업**한 뒤 삭제 진행
  ```bash
  # .env.local 백업 (있을 경우만)
  [ -f c:/work/jione-portfolio/.env.local ] && cp c:/work/jione-portfolio/.env.local c:/work/portfolio-agent/.env.local
  # .git 제외 전체 삭제
  find c:/work/jione-portfolio -mindepth 1 -maxdepth 1 ! -name ".git" -exec rm -rf {} +
  ```
- **옵션 2 선택 시**: Step 1~2를 건너뛰고 Step 3부터 진행. 이후 `/content-setup`을 실행하도록 안내

---

### 1. 프로젝트 생성

```bash
cd /c/work
npx create-next-app@latest jione-portfolio --typescript

cd c:/work/jione-portfolio
```

설정 선택사항:

```
✔ TypeScript: Yes
✔ ESLint: Yes
✔ Tailwind CSS: No
✔ App Router: Yes
✔ Src/ directory: Yes
✔ Import alias: Yes (@/* = src/*)
```

### 2. 필수 의존성 설치

```bash
npm install \
  framer-motion \
  @tanstack/react-query \
  recoil \
  clsx \
  styled-components \
  next-image-export-optimizer \
  web-vitals

npm install --save-dev @types/styled-components
```

### 3. 폴더 구조 생성

```
src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx                      # 모든 라우트 링크 랜딩
│   ├── {style}/{tone}/               # 전체 조합 자동 생성
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── admin/
│   │   ├── layout.tsx
│   │   └── page.tsx
│   └── api/admin/logs/
│       ├── route.ts
│       └── clear/route.ts
├── components/
│   ├── block/                        # Step 4에서 생성
│   └── corporate/                    # Step 4에서 생성
├── lib/
│   └── memory-db.ts
├── actions/
│   └── logging.ts
└── styles/
    ├── globals.css
    ├── styled.d.ts
    ├── provider.tsx
    └── themes/                       # Step 5에서 생성

public/
├── robots.txt
├── sitemap.xml
└── images/
```

```bash
mkdir -p data
mkdir -p src/styles/themes
mkdir -p src/lib src/actions
mkdir -p public/images
```

---

### 3-1. 공용 인프라 파일 생성

**`src/styles/styled.d.ts`** — DefaultTheme 타입 확장:

```typescript
import "styled-components";

declare module "styled-components" {
  export interface DefaultTheme {
    colors: {
      background: string;
      foreground: string;
      primary: string;
      primaryForeground: string;
      secondary: string;
      secondaryForeground: string;
      muted: string;
      mutedForeground: string;
      border: string;
      card: string;
      cardForeground: string;
      destructive: string;
      [key: string]: string;
    };
    radius: {
      xs: string;
      sm: string;
      md: string;
      lg: string;
      xl: string;
    };
    shadows: {
      sm: string;
      md: string;
      lg: string;
    };
    fonts: {
      primary: string;
    };
    typography: {
      h1: {
        fontSize: string;
        fontWeight: number;
        letterSpacing?: string;
        lineHeight?: number;
      };
      h2: {
        fontSize: string;
        fontWeight: number;
        letterSpacing?: string;
        lineHeight?: number;
      };
      body: {
        fontSize: string;
        fontWeight: number;
        lineHeight?: number;
        letterSpacing?: string;
      };
      small: { fontSize: string; fontWeight: number; letterSpacing?: string };
    };
  }
}
```

**`src/styles/provider.tsx`** — SSR StyleProvider:

```typescript
'use client';

import React, { useState } from 'react';
import { useServerInsertedHTML } from 'next/navigation';
import { ServerStyleSheet, StyleSheetManager, ThemeProvider } from 'styled-components';
import type { DefaultTheme } from 'styled-components';

export function StyleProvider({
  children,
  theme,
}: {
  children: React.ReactNode;
  theme: DefaultTheme;
}) {
  const [styledComponentsStyleSheet] = useState(() => new ServerStyleSheet());

  useServerInsertedHTML(() => {
    const styles = styledComponentsStyleSheet.getStyleElement();
    styledComponentsStyleSheet.instance.clearTag();
    return <>{styles}</>;
  });

  return typeof window !== 'undefined' ? (
    <ThemeProvider theme={theme}>{children}</ThemeProvider>
  ) : (
    <StyleSheetManager sheet={styledComponentsStyleSheet.instance}>
      <ThemeProvider theme={theme}>{children}</ThemeProvider>
    </StyleSheetManager>
  );
}
```

**`src/styles/globals.css`** — 전역 CSS 리셋:

```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family:
    "Pretendard Variable",
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

a {
  text-decoration: none;
  color: inherit;
}
button {
  border: none;
  cursor: pointer;
  background: none;
  font-family: inherit;
}
input,
textarea {
  font-family: inherit;
}
```

---

### 4. 스타일 컴포넌트 전체 생성

`.claude/commands/2[a-z]*.md` 파일을 Glob으로 모두 스캔합니다.
**각 파일의 Steps를 순서대로 실행**하여 해당 스타일의 컴포넌트를 생성합니다.

| 파일                        | 대상 컴포넌트 경로          |
| --------------------------- | --------------------------- |
| `2a.style-block.md`         | `src/components/block/`     |
| `2b.style-corporate.md`     | `src/components/corporate/` |
| 이후 추가된 `2c.*`, `2d.*`… | `src/components/{style}/`   |

> 새 스타일이 추가될 때는 `2[a-z]*.md` 파일을 추가하기만 하면 다음 `/project-init` 실행 시 자동 포함됩니다.

---

### 5. 색상 토큰 전체 생성

`.claude/commands/3[a-z]*.md` 파일을 Glob으로 모두 스캔합니다.
**각 파일에 정의된 토큰을 `src/styles/themes/{tone}.ts`로 생성**합니다.

| 파일                 | 생성 파일                      |
| -------------------- | ------------------------------ |
| `3a.tone-toss.md`    | `src/styles/themes/toss.ts`    |
| `3d.tone-kakao.md`   | `src/styles/themes/kakao.ts`   |
| 이후 추가된 `3e.*`…  | `src/styles/themes/{tone}.ts`  |

---

### 6. 전체 라우트 생성 (Style × Tone 매트릭스)

Step 4에서 확인한 **Style 목록** × Step 5에서 확인한 **Tone 목록**으로 모든 조합을 생성합니다.

**각 조합마다 두 파일 생성:**

#### `src/app/{style}/{tone}/layout.tsx`

```typescript
'use client';
import { StyleProvider } from '@/styles/provider';
import { {tone}Theme } from '@/styles/themes/{tone}';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <StyleProvider theme={{tone}Theme}>{children}</StyleProvider>;
}
```

#### `src/app/{style}/{tone}/page.tsx`

> ⚠️ **빌드 통과용 빈 스텁만 생성** — 실제 데이터는 `/sample-page-create` 실행 시 `data/resume.ts`를 읽어 하드코딩됩니다.

**block 스타일 (빌드 스텁):**

```typescript
'use client';

// ⚠️ 이 파일은 빌드 통과용 스텁입니다.
// /sample-page-create 를 실행하면 data/resume.ts 값으로 교체됩니다.

import styled from 'styled-components';
import { HeroSection } from '@/components/block/hero';
import { AboutSection } from '@/components/block/about';
import { ProjectsSection } from '@/components/block/projects';
import { ContactSection } from '@/components/block/contact';

const Main = styled.main`
  width: 100%;
  overflow-x: hidden;
`;

export default function Page() {
  return (
    <Main>
      <HeroSection name="" title="" subtitle="" bio="" />
      <AboutSection experiences={[]} skillCategories={[]} />
      <ProjectsSection intro="" cases={[]} />
      <ContactSection email="" />
    </Main>
  );
}
```

**corporate 스타일 (빌드 스텁):**

```typescript
'use client';

// ⚠️ 이 파일은 빌드 통과용 스텁입니다.
// /sample-page-create 를 실행하면 data/resume.ts 값으로 교체됩니다.

import { GNB } from '@/components/corporate/gnb';
import { Hero } from '@/components/corporate/hero';
import { CardSlider } from '@/components/corporate/card-slider';
import { Career } from '@/components/corporate/career';
import { Contact } from '@/components/corporate/contact';

export default function Page() {
  return (
    <>
      <GNB name="" github="" email="" />
      <Hero title="" name="" subtitle="" />
      <CardSlider cases={[]} />
      <Career experiences={[]} skillCategories={[]} />
      <Contact name="" bio="" email="" />
    </>
  );
}
```

---

### 7. 핵심 지원 파일 생성

**`data/resume.ts`** — portfolio-agent 루트에 이미 존재합니다. `c:/work/jione-portfolio/` 안에 생성하지 않습니다.

> `data/resume.ts`는 portfolio-agent 루트(`c:\work\portfolio-agent\data\resume.ts`)에 위치하며, commands가 읽는 단일 소스입니다.
> `/sample-page-create` 실행 시 이 파일을 읽어 page.tsx에 하드코딩합니다.
> 컴포넌트에서 직접 import하지 않습니다.

아래는 `data/resume.ts`의 구조 참고용입니다 (수정은 portfolio-agent 루트에서):

```typescript
// data/resume.ts (portfolio-agent 루트)
// ⚠️ 수정 후 /sample-page-create 로 page.tsx를 재생성하세요.

export const resume = {
  meta: {
    siteTitle: '포트폴리오',
    siteDescription: 'Full-Stack Engineer 포트폴리오',
    siteUrl: 'https://example.com',
    ogImage: '/images/og.png',
    author: '이름',
    keywords: ['포트폴리오', '개발자'],
    theme: 'toss',
  },
  profile: {
    name: '이름',
    title: 'Full-Stack Engineer',
    subtitle: '한 줄 소개',
    email: 'hello@example.com',
    phone: '010-0000-0000',
    location: '서울',
    availability: '협업 가능',
    bio: '자기소개를 입력하세요.',
    image: '/images/profile.jpg',
    social: { github: 'https://github.com', linkedin: '' },
  },
  career: {
    summary: '경력 요약',
    experiences: [
      {
        id: 'exp-1',
        company: '회사명',
        position: '직책',
        period: { start: '2022.01', end: 'present' },
        description: '담당 업무',
        achievements: ['성과 1'],
      },
    ],
  },
  Project: {
    intro: '프로젝트 소개',
    cases: [
      {
        id: 'project-1',
        title: '프로젝트명',
        description: '설명',
        execution: 'Next.js, TypeScript',
        tools: '',
        year: '2024',
        images: [],
      },
    ],
  },
  skills: {
    categories: [
      {
        name: 'Frontend',
        skills: [
          { name: 'React', level: 'expert' },
          { name: 'Next.js', level: 'expert' },
        ],
      },
    ],
  },
} as const;

export type Resume = typeof resume;
export type Experience = (typeof resume.career.experiences)[number];
export type SkillCategory = (typeof resume.skills.categories)[number];
export type ProjectCase = (typeof resume.Project.cases)[number];
```

**`src/lib/memory-db.ts`**:

```typescript
interface PageLog {
  id: number;
  url: string;
  path: string;
  referrer?: string;
  userAgent?: string;
  ipAddress?: string;
  timestamp: Date;
}

let logs: PageLog[] = [];
let nextId = 1;

export const memoryDB = {
  addLog: (data: Omit<PageLog, "id" | "timestamp">) => {
    const log: PageLog = { id: nextId++, ...data, timestamp: new Date() };
    logs.push(log);
    return log;
  },
  getLogs: (limit: number = 100) =>
    logs
      .sort((a, b) => b.timestamp.getTime() - a.timestamp.getTime())
      .slice(0, limit),
  getStats: () => {
    const stats: Record<string, { count: number; lastVisit: Date }> = {};
    logs.forEach((log) => {
      if (!stats[log.path])
        stats[log.path] = { count: 0, lastVisit: log.timestamp };
      stats[log.path].count++;
      if (log.timestamp > stats[log.path].lastVisit)
        stats[log.path].lastVisit = log.timestamp;
    });
    return stats;
  },
};
```

**`src/actions/logging.ts`**:

```typescript
"use server";

import { cookies } from "next/headers";
import { memoryDB } from "@/lib/memory-db";

export async function logPageView() {
  try {
    const cookieStore = await cookies();
    const pageViewData = cookieStore.get("_pageViewData")?.value;
    if (!pageViewData) return;
    const data = JSON.parse(pageViewData);
    memoryDB.addLog({
      url: data.url,
      path: data.path,
      referrer: data.referrer,
      userAgent: data.userAgent,
      ipAddress: data.ipAddress,
    });
  } catch (error) {
    console.error("Failed to log page view:", error);
  }
}
```

---

### 8. 루트 레이아웃 및 랜딩 페이지

**`src/app/layout.tsx`**:

```typescript
import type { Metadata } from 'next';
import '@/styles/globals.css';

export const metadata: Metadata = {
  title: '포트폴리오',
  description: 'Full-Stack Engineer 포트폴리오',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <head>
        <link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.css" rel="stylesheet" />
        <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet" />
      </head>
      <body>{children}</body>
    </html>
  );
}
```

**`src/app/page.tsx`** — 모든 라우트 링크 랜딩 페이지:

Step 4~5에서 생성한 Style × Tone 목록을 기반으로 동적 생성합니다.
Style별로 그룹화하여 각 그룹이 하나의 카드로 표시됩니다.

```typescript
const groups = [
  {
    style: 'Block',
    description: '카드 기반 섹션 구성 — Hero → About → Projects → Contact',
    tones: [
      { label: 'Toss', path: '/block/toss', primary: '#3182F6', fg: '#ffffff' },
      { label: 'Kakao', path: '/block/kakao', primary: '#FEE500', fg: '#1A1A1A' },
    ],
  },
  {
    style: 'Corporate',
    description: 'GNB + 대형 Hero + 카드 슬라이더 구성 — 기업 사이트 스타일',
    tones: [
      { label: 'Toss', path: '/corporate/toss', primary: '#3182F6', fg: '#ffffff' },
      { label: 'Kakao', path: '/corporate/kakao', primary: '#FEE500', fg: '#1A1A1A' },
    ],
  },
];

export default function LandingPage() {
  return (
    <main style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', flexDirection: 'column', gap: '2rem', fontFamily: 'Pretendard Variable, sans-serif', background: '#f8f9fa', padding: '3rem 2rem' }}>
      <h1 style={{ fontSize: '2rem', fontWeight: 700, letterSpacing: '-0.02em' }}>포트폴리오 스타일 선택</h1>
      <p style={{ color: '#888' }}>원하는 Style × Tone 조합을 선택하세요.</p>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', maxWidth: '640px', width: '100%' }}>
        {groups.map((g) => (
          <div key={g.style} style={{ background: '#ffffff', borderRadius: '16px', padding: '1.75rem', boxShadow: '0 2px 12px rgba(0,0,0,0.07)' }}>
            <p style={{ fontSize: '0.75rem', fontWeight: 600, color: '#aaa', letterSpacing: '0.08em', textTransform: 'uppercase', marginBottom: '0.25rem' }}>Style</p>
            <h2 style={{ fontSize: '1.25rem', fontWeight: 700, marginBottom: '0.375rem' }}>{g.style}</h2>
            <p style={{ fontSize: '0.875rem', color: '#888', marginBottom: '1.25rem' }}>{g.description}</p>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: '0.75rem' }}>
              {g.tones.map((t) => (
                <a key={t.path} href={t.path} style={{ padding: '0.75rem 0', background: t.primary, color: t.fg, borderRadius: '10px', textAlign: 'center', fontWeight: 600, fontSize: '0.9rem', textDecoration: 'none', display: 'block' }}>
                  {t.label}
                </a>
              ))}
            </div>
          </div>
        ))}
      </div>
    </main>
  );
}
```

---

### 9. next.config.ts 및 tsconfig.json 업데이트

**`next.config.ts`**:

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  compiler: {
    styledComponents: true,
  },
};

export default nextConfig;
```

**`tsconfig.json`** — `paths` 설정 (`@data/*` 불필요):

```json
"paths": {
  "@/*": ["./src/*"]
}
```

> 컴포넌트는 `data/resume.ts`를 import하지 않습니다. `data/resume.ts`는 commands가 읽는 소스이며, 값은 `/sample-page-create` 실행 시 page.tsx에 하드코딩됩니다.

### 10. 환경 변수 설정

**`.env.local.example`** 생성:

```env
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Your Portfolio
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=

# NextAuth (NEXTAUTH_SECRET: openssl rand -base64 32)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# 카카오 OAuth (developers.kakao.com)
KAKAO_CLIENT_ID=your_kakao_rest_api_key
KAKAO_CLIENT_SECRET=your_kakao_client_secret

# Google Gemini API (aistudio.google.com)
GEMINI_API_KEY=your_gemini_api_key

DATABASE_URL=
ADMIN_PASSWORD=
```

**`.env.local`** 복원 또는 생성 (루트에 백업본 우선):

```bash
# portfolio-agent 루트의 .env.local 백업본이 있으면 복원, 없으면 example 복사
if [ -f c:/work/portfolio-agent/.env.local ]; then
  cp c:/work/portfolio-agent/.env.local .env.local
  echo "✅ .env.local 복원 완료 (루트 백업본 사용)"
else
  cp .env.local.example .env.local
  echo "ℹ️  .env.local 새로 생성됨 — API 키를 직접 입력하세요"
fi
```

### 11. Prettier 설정

**`.prettierrc`**:

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "arrowParens": "always"
}
```

### 12. 빌드 및 검증

```bash
cd c:/work/jione-portfolio && npm run build
npm run dev
```

- `http://localhost:3000` — 스타일 선택 랜딩 확인
- `http://localhost:3000/block/toss` 등 각 라우트 확인

### 13. Git 초기화 (선택)

```bash
git init
git add .
git commit -m "Initial portfolio: all styles generated

- Next.js 16 + TypeScript + App Router
- styled-components (no Tailwind)
- All style × tone routes generated
- memory-db logging
"
git remote add origin <your-github-repo-url>
git branch -M main
git push -u origin main
```

---

## Rules

- **패키지 매니저**: npm
- **Node 버전**: 18.17 이상
- **빌드 성공**: `npm run build` 에러 없음 필수
- **스타일 참조**: 항상 `2[a-z]*.md` 파일을 읽어서 컴포넌트 생성 (코드 직접 작성 금지)
- **토큰 참조**: 항상 `3[a-z]*.md` 파일을 읽어서 테마 파일 생성

---

## 완료 보고

```
✅ Next.js 프로젝트 생성
✅ 필수 의존성 설치
✅ 공용 인프라 생성 (provider.tsx, styled.d.ts, globals.css)
✅ 스타일 컴포넌트 생성: block, corporate (2x.*.md 기준)
✅ 색상 토큰 생성: toss, kakao (3x.*.md 기준)
✅ 라우트 생성: /block/toss, /block/kakao,
               /corporate/toss, /corporate/kakao
✅ 랜딩 페이지 생성 (전체 라우트 링크)
✅ npm run build 성공

→ http://localhost:3000 에서 스타일 선택 가능
```

---

## 다음 단계

### 🟢 Phase 1 (필수) — 콘텐츠 입력

```
/content-setup   ← resume.ts 콘텐츠 입력 (모든 페이지 공통)
```

### 🔵 Phase 2 (선택)

```
/seo-optimize          ← SEO 메타데이터
/vercel-deploy         ← Vercel 배포
/admin-logging-setup   ← 방문자 분석
/monitoring-setup      ← Google Analytics
```

> 개별 테마를 나중에 추가하려면: `/theme-on {style}/{tone}`
