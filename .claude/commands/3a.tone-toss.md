---
description: Toss 톤앤매너 색상 토큰을 설치합니다 (선행: /project-init)
argument-hint: "<강조 포인트 컬러 (선택, 기본: #3182F6)>"
---

🟢 **Phase 1 — 톤앤매너 · Toss**

> 📌 공용 인프라(`provider.tsx`, `styled.d.ts`, `globals.css`), 공통 Rules, 빌드 확인은 **`2.theme-on.md`** 참조

## Toss 톤 특징

| 항목 | 값 | 비고 |
|------|----|------|
| 배경 | `#FFFFFF` | 깨끗한 흰색 |
| 강조색 | `#3182F6` | Toss Blue |
| 모서리 | `12px` | Toss 특유의 둥근 모서리 |
| 타이포 | Bold weight | 강한 시각적 계층 |
| 그림자 | 미세한 레이어 | 카드 입체감 |

## 생성할 파일

| 파일 | 역할 |
|------|------|
| `src/styles/themes/toss.ts` | Toss 디자인 토큰 |

## Steps

### Step 1: `src/styles/themes/toss.ts` 생성

```typescript
export const tossTheme = {
  colors: {
    background: "#FFFFFF",
    foreground: "#1B1C1F",
    primary: "#3182F6", // Toss Blue (또는 $ARGUMENTS로 커스텀)
    primaryForeground: "#FFFFFF",
    secondary: "#F2F4F6",
    secondaryForeground: "#4E5968",
    muted: "#F9FAFB",
    mutedForeground: "#8B95A1",
    border: "#E5E8EB",
    card: "#FFFFFF",
    cardForeground: "#1B1C1F",
    destructive: "#FF4D4F",
  },

  radius: {
    xs: "0.25rem",
    sm: "0.5rem",
    md: "0.75rem", // 12px — Toss 특유의 둥근 모서리
    lg: "1rem",
    xl: "1.5rem",
  },

  shadows: {
    sm: "0 1px 4px rgba(0,0,0,0.06)",
    md: "0 4px 16px rgba(0,0,0,0.08)",
    lg: "0 8px 32px rgba(0,0,0,0.10)",
  },

  fonts: {
    primary:
      "'Pretendard Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif",
  },

  typography: {
    h1: { fontSize: "2.25rem", fontWeight: 700, letterSpacing: "-0.02em" },
    h2: { fontSize: "1.875rem", fontWeight: 700, letterSpacing: "-0.02em" },
    body: { fontSize: "1rem", fontWeight: 400, lineHeight: 1.5 },
    small: { fontSize: "0.875rem", fontWeight: 400 },
  },
} as const;

export type TossTheme = typeof tossTheme;
```

## 스타일 가이드 힌트

- **카드**: `background: ${props => props.theme.colors.card}`, `border-radius: ${props => props.theme.radius.md}`
- **강조 버튼**: `background: ${props => props.theme.colors.primary}` + `color: ${props => props.theme.colors.primaryForeground}`
- **섹션 구분**: 배경색 alternating (`background` ↔ `muted`)
- **타이포**: `letter-spacing: -0.02em` + `fontWeight: 700`
