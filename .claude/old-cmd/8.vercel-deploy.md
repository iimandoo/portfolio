---
description: Vercel에 포트폴리오를 배포하고 자동 배포 파이프라인을 설정합니다 (선행: /seo-optimize)
argument-hint: "<배포 환경 (선택, preview|production), 기본: production>"
---

🔵 **Phase 2 — 배포 & 운영 최적화** (Phase 1 완료 후 진행)

포트폴리오를 Vercel에 배포하고, GitHub 연동으로 자동 배포 파이프라인을 설정합니다.

⚠️ **사전 조건**: Phase 1 완료 필수. `/seo-optimize`를 먼저 실행하면 SEO도 함께 배포됩니다 (선택).

## 목표

- Vercel 프로젝트 생성 및 배포
- GitHub 자동 배포 설정 (CI/CD)
- 환경 변수 설정 (프로덕션, 미리보기)
- 커스텀 도메인 설정 (선택)
- 배포 모니터링 및 롤백 설정

## Steps

### 1. GitHub 저장소 준비

#### 1-1. GitHub에 저장소 생성

1. GitHub 접속 → "New repository"
2. Repository name: `jione-portfolio`
3. Public 선택 (포트폴리오는 공개)
4. Add a README.md 체크 (선택)
5. Create repository

#### 1-2. 로컬 프로젝트를 GitHub에 푸시

```bash
cd jione-portfolio

# Git 초기화 (이미 했다면 스킵)
git init
git add .
git commit -m "Initial jione-portfolio setup

- Next.js 16 with App Router
- Toss Design System
- Content data structure
- SEO optimization
"

# GitHub 저장소 연동
git remote add origin https://github.com/YOUR_USERNAME/jione-portfolio.git
git branch -M main
git push -u origin main
```

### 2. .gitignore 확인

**`.gitignore`** 다음 항목 포함 확인:

```
# Next.js
.next/
out/
*.pem

# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build
dist/
build/
.turbo/

# Testing
coverage/
.nyc_output/

# Database
data/
*.db
*.sqlite
```

### 3. Vercel 계정 생성 및 설정

1. [Vercel 공식 사이트](https://vercel.com) 접속
2. "Sign Up" → GitHub 계정으로 로그인
3. Vercel GitHub App 설치 및 인증
4. 프로젝트 생성 또는 기존 저장소 임포트

### 4. Vercel에 프로젝트 배포

#### 옵션 A: Vercel UI에서 배포 (권장)

1. [Vercel Dashboard](https://vercel.com/dashboard) 접속
2. "Add New..." → "Project"
3. GitHub 저장소 선택 (`jione-portfolio`)
4. Import Project 클릭

#### 옵션 B: Vercel CLI로 배포

```bash
npm install -g vercel

# Vercel 로그인
vercel login

# 프로젝트 디렉토리에서 배포
cd jione-portfolio
vercel

# 프로덕션 배포
vercel --prod
```

### 5. Vercel 프로젝트 설정

#### 5-1. 빌드 설정 확인

**Vercel Dashboard → Settings → Build & Development Settings**

```
Framework Preset: Next.js
Build Command: npm run build
Output Directory: .next
Install Command: npm ci
Development Command: npm run dev
```

#### 5-2. 환경 변수 설정

**Vercel Dashboard → Settings → Environment Variables**

다음 변수 추가:

```
NEXT_PUBLIC_SITE_URL = https://jione-portfolio.vercel.app (프로덕션)
NEXT_PUBLIC_SITE_NAME = Your Portfolio Name
NEXT_PUBLIC_GA_ID = (Google Analytics ID, 선택)
NEXT_PUBLIC_VERCEL_ANALYTICS_ID = (Vercel Analytics, 자동 생성)
ADMIN_PASSWORD = (Admin 대시보드 암호, 선택)
```

#### 5-3. 배포 보호 (선택)

**Settings → Deployment Protection**

- Production: Protected (배포 전 승인 필요)
- Preview: Unprotected (즉시 배포)

### 6. vercel.json 설정

**`vercel.json`** 생성:

```json
{
  "version": 2,
  "buildCommand": "npm run build",
  "public": true,
  "env": [
    "NEXT_PUBLIC_SITE_URL",
    "NEXT_PUBLIC_SITE_NAME",
    "NEXT_PUBLIC_GA_ID"
  ],
  "enviroments": {
    "production": {
      "env": ["NEXT_PUBLIC_SITE_URL"]
    }
  },
  "headers": [
    {
      "source": "/images/:path*",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/fonts/:path*",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/api/:path*",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, s-maxage=60, stale-while-revalidate=120"
        }
      ]
    }
  ],
  "redirects": [],
  "rewrites": []
}
```

### 7. GitHub Actions 자동 배포 파이프라인 (선택)

**`.github/workflows/deploy.yml`** 생성:

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint --if-present

      - name: Build
        run: npm run build

      - name: Deploy to Vercel
        uses: vercel/action@master
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          scope: ${{ secrets.VERCEL_ORG_ID }}
```

#### GitHub Secrets 설정

1. GitHub 저장소 → Settings → Secrets and variables → Actions
2. "New repository secret" 추가:
   - `VERCEL_TOKEN`: Vercel 계정 토큰
   - `VERCEL_ORG_ID`: Vercel Organization ID
   - `VERCEL_PROJECT_ID`: Vercel Project ID

**토큰 및 ID 얻기:**
```bash
vercel login
vercel link --yes
vercel env pull .env.local
```

생성된 정보는 Vercel 계정 설정에서 확인 가능

### 8. 커스텀 도메인 설정 (선택)

**Vercel Dashboard → Settings → Domains**

1. "Add Domain" 클릭
2. 도메인 입력 (예: jione-portfolio.vercel.app)
3. DNS 설정 완료

**DNS 레코드 추가:**
```
Type: CNAME
Name: www
Value: cname.vercel.app
```

또는 루트 도메인:
```
Type: A
Name: @
Value: 76.76.19.165
```

### 9. 배포 후 검증

#### 9-1. 배포 확인

1. Vercel Dashboard에서 배포 상태 확인
2. 프로덕션 URL 접속
3. 페이지 정상 로드 확인

#### 9-2. 성능 모니터링

**Vercel Analytics 활성화:**
- Vercel Dashboard → Settings → Analytics
- "Enable Vercel Analytics" 클릭

**Lighthouse 검사:**
```bash
npm install -g lighthouse

lighthouse https://yourjione-portfolio.vercel.app \
  --view \
  --chrome-flags="--headless --no-sandbox"
```

#### 9-3. 보안 검사

다음 도구로 확인:
- [Security Headers](https://securityheaders.com)
- [SSL Labs](https://www.ssllabs.com/ssltest/)
- [Mozilla Observatory](https://observatory.mozilla.org/)

### 10. 배포 전 체크리스트

```
✅ npm run build 성공 (로컬)
✅ npm run dev 정상 작동
✅ 환경 변수 설정 완료
✅ .gitignore에 민감한 파일 제외
✅ package.json의 scripts 정상
✅ API 라우트 테스트 완료
✅ 모바일 반응형 확인
✅ 이미지 최적화 확인
✅ Meta 태그 확인
✅ robots.txt, sitemap.xml 정상
```

## 배포 후 운영 가이드

### 자동 배포 프로세스

```
코드 변경 → Git Push → GitHub → Vercel → 자동 배포
```

1. **Feature 브랜치에서 개발**
   ```bash
   git checkout -b feature/new-page
   # 코드 작성
   git add .
   git commit -m "Add new page"
   ```

2. **Pull Request 생성**
   - GitHub에서 Pull Request 생성
   - Vercel이 자동으로 Preview URL 생성
   - 테스트 및 검수

3. **Main 브랜치에 Merge**
   - Merge 클릭
   - Vercel이 자동으로 프로덕션 배포

4. **배포 모니터링**
   - Vercel Dashboard에서 배포 상태 확인
   - 에러 발생 시 자동 알림

### 롤백

배포 후 문제 발견 시:
1. Vercel Dashboard → Deployments
2. 이전 정상 배포 버전 선택
3. "Promote to Production" 클릭

## 커스텀 헤더 및 보안 설정

**`next.config.js`** 보안 헤더 추가:

```javascript
const securityHeaders = [
  {
    key: 'X-Content-Type-Options',
    value: 'nosniff',
  },
  {
    key: 'X-Frame-Options',
    value: 'SAMEORIGIN',
  },
  {
    key: 'X-XSS-Protection',
    value: '1; mode=block',
  },
  {
    key: 'Referrer-Policy',
    value: 'strict-origin-when-cross-origin',
  },
  {
    key: 'Strict-Transport-Security',
    value: 'max-age=31536000; includeSubDomains',
  },
];

const nextConfig = {
  async headers() {
    return [
      {
        source: '/:path*',
        headers: securityHeaders,
      },
    ];
  },
};

module.exports = nextConfig;
```

## Rules

- **배포 브랜치**: main 브랜치만 프로덕션 배포
- **환경 변수**: Vercel Dashboard에서만 설정 (코드에 포함 금지)
- **빌드 성공**: 항상 로컬에서 `npm run build` 테스트 후 푸시
- **성능**: Vercel Analytics로 Core Web Vitals 모니터링
- **도메인**: 커스텀 도메인 사용 권장 (전문성 향상)

## 완료 보고

Vercel 배포 완료 후:
```
✅ Vercel 프로젝트 생성
✅ GitHub 자동 배포 설정
✅ 환경 변수 설정 완료
✅ 커스텀 도메인 설정 (선택)
✅ Security Headers 설정
✅ 프로덕션 배포 성공
✅ Lighthouse 점수 확인

다음 명령어: /monitoring-setup
```

## 다음 단계

1. **`/admin-logging-setup`** → 방문자 분석 기능 추가 (이미 설정했다면 스킵)
2. **`/monitoring-setup`** → Google Analytics, 성능 모니터링 설정
3. **정기 점검**:
   - 주 1회 Vercel Analytics 확인
   - 월 1회 Google Search Console 확인
   - 월 1회 Lighthouse 검사
